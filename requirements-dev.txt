# Development dependencies for NSM
# Install with: pip install -r requirements-dev.txt

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-xdist>=3.0.0  # For parallel test execution

# Code quality
black>=22.0.0
isort>=5.0.0  # configured with profile=black in pyproject.toml; `make lint` checks it
flake8>=5.0.0
# The D1xx half of `make lint`: every public module, class, method and function
# carries a docstring. See .flake8 for which families are on and why the rest are not.
flake8-docstrings>=1.7.0
mypy>=1.0.0

# Documentation
pdoc>=14.0.0  # `make docs` -- API reference from docstrings
sphinx>=5.0.0
sphinx-rtd-theme>=1.0.0

# Development tools
jupyter>=1.0.0
ipykernel>=6.0.0

# Scientific computing (if not in main requirements)
numpy>=1.20.0
scipy>=1.7.0
matplotlib>=3.5.0

# Additional testing utilities
parameterized>=0.8.1  # For parameterized tests

# Development utilities
pre-commit>=2.20.0  # For git hooks

# Packaging. Both are build requirements (pyproject.toml) and are needed at development
# time too, because testing/NSM/test_packaging.py builds a wheel with --no-build-isolation
# and that uses the ambient copies rather than a fresh isolated environment.
setuptools>=61
setuptools-scm>=7.0.0